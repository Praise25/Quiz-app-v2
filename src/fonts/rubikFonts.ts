import { Rubik } from "next/font/google";

export const rubikLight = Rubik({
  weight: "300",
});

export const rubikMedium = Rubik({
  weight: "500",
});

export const rubikItalic = Rubik({
  weight: "400",
  style: "italic",
});